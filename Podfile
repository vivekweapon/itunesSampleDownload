# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iTunesSampleDownload' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'Kingfisher', '~> 4.0'

  # Pods for iTunesSampleDownload

  target 'iTunesSampleDownloadTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iTunesSampleDownloadUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
